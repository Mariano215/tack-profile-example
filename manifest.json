{
  "schema_version": 2,
  "name": "example",
  "description": "Starter profile. Copy this repo, rename it, edit the manifest.",
  "mcp": [],
  "memory": {
    "enabled": false
  },
  "providers": {
    "claude": {
      "plugins": {
        "superpowers@claude-plugins-official": true
      },
      "env": {}
    },
    "codex": {}
  },
  "notes": "Nothing here is required. Delete every key you do not want; name and description are the only two the engine reads unconditionally."
}
